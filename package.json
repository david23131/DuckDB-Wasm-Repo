{
  "name": "duckdb-opfs-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "build": "vite build",
    "test": "vitest run",
    "prepare:nfcorpus": "node scripts/prepare-nfcorpus.mjs"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "1.32.0",
    "@huggingface/transformers": "4.2.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "3.2.4"
  }
}
